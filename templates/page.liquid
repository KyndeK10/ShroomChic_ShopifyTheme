<!-- Default Page Template -->
<div class="container py-24" style="max-width:56rem">
  <div class="text-center mb-12">
    <h1 class="font-serif gradient-text" style="font-size:2.5rem;font-weight:700;margin-bottom:1.5rem">{{ page.title }}</h1>
  </div>
  <div class="about-text">
    {{ page.content }}
  </div>
</div>
